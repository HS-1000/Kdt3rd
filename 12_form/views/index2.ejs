<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <form action="/getForm">
    이름<input type="text" name="userName">
    <fieldset>
      <legend>성별</legend>
      <input type="radio" name="male">남자 
      <input type="radio" name="female">여자
    </fieldset>
    <br>
    <fieldset>
      <legend>생년월일</legend>
      <select name="birthDayYear">
        <% for(var i = 2023; i > 1900; i--) { %>
          <option><%= i %></option>
        <% } %>
      </select>년
      <select name="birthDayYear">
        <% for(var i = 1; i <= 12; i++) { %>
          <option><%= i %></option>
        <% } %>
      </select>월
      <select name="birthDayYear">
        <% for(var i = 1; i <= 31; i++) { %>
          <option><%= i %></option>
        <% } %>
      </select>일
    </fieldset>
    <br>
    <fieldset>
      <legend>관심사</legend>
      <input type="checkbox" name="interest1"> 여행
      <input type="checkbox" name="interest2"> 패션
      <input type="checkbox" name="interest3"> 음식
    </fieldset>
    <br>
    <button>회원가입</button>
  </form>
</body>
</html>